#include <stdio.h>
#include <stdlib.h>

int main () {

    float pippo, gigi;
    printf("inserisci un numero");
    scanf ("%f");
    {
    if (pippo >= gigi)
    printf("il numero %f è maggiore o uguale di %f", pippo, gigi);
    }
    {
    else
    printf("il numero %f è maggiore o uguale di %f", gigi, pippo);
        
    }
    
}